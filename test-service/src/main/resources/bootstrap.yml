spring:
  application:
    name: test-service
  cloud:
    config:
      enabled: false
server:
  port: 1001
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    metadata-map:
      instanceId: ${spring.application.name}:${random.value}