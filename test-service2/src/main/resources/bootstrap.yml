spring:
  application:
    name: test-service2
server:
  port: 1002
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-service:8761/eureka/
  instance:
    prefer-ip-address: true
turbine:
  cluster-name-expression: 'default'